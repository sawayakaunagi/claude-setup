# NOTICE — 帰属と参照

このリポジトリのコード・文書は MIT（[LICENSE](./LICENSE)）で配布しています。
以下は**他者の成果物への帰属**と、**参照しているが同梱していないもの**の整理です。

---

## 設計原則の語彙について

本キットのスキル文書は、設計判断の根拠を `C-1` / `V-1.2` / `S-1.3` のような**原則ID**で参照しています。

この ID 体系は **`agent-essence`（AIエージェント設計の本質）**——
まさお氏によるハーネス設計原則集 —— のものです。
`audit-harness`（同氏によるハーネス診断スキル）も同じ体系を使っており、
**本キットの生成物をそのまま診断にかけられる**のはこのためです。

本リポジトリに含まれる**文章はすべて書き下ろし**で、`agent-essence` 本文や
`diagnosis-rubric.md` の文面は含んでいません。参照しているのは ID 体系と考え方です。

`agent-essence` / `audit-harness` / `diagnosis-rubric.md` は本キットに同梱していません。
入手は配布元に従ってください。

本キットの設計は、同氏のハーネスエンジニアリングに関する一連の記事・勉強会
（AI駆動開発研究部）から多くの知見を得ています。本リポジトリの文章はすべて書き下ろしで、
記事本文の転載は含みませんが、考え方の系譜として記しておきます。

---

## 参照しているが同梱していないプラグイン

`/setup-project` の Phase 4 は、以下を**検出して提示するだけ**です。
同梱もインストールもしません。

| プラグイン | 作者 | ライセンス |
|---|---|---|
| [eval-loop](https://github.com/AI-Driven-R-D-Dept) | Masa | MIT |
| [akapen](https://github.com/AI-Driven-R-D-Dept/akapen) | kyohei（bond-ai.co.jp） | MIT |
| bestofn | AI-Driven-R-D-Dept | 表記なし |

### なぜ MIT のものも同梱しないのか

ライセンス上は可能ですが、**技術的に衝突するため**です。

これらは既にそれぞれ独立した marketplace として配布されています。
本キットの `marketplace.json` が同名のプラグインを重ねて宣言すると、
既にインストールしているユーザーの環境で重複した供給元ができてしまいます。

加えて、同梱はフォークと同じで、**本家が更新されても追随しません**。
参照にしておけば、本家の更新がそのまま効きます。

---

## 手元でだけ同梱したい場合

`local/` を使ってください（`.gitignore` 済みで、公開リポジトリには入りません）。
詳細は [local/README.md](./local/README.md) を参照してください。
